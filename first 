import datetime

print("🤖 Rest AI Chatbot")
print("Type 'exit' to quit.\n")

memory = {}

while True:
    user = input("You: ")

    if user.lower() == "exit":
        print("Bot: Goodbye!")
        break

    # Remember name
    if "my name is" in user.lower():
        name = user.split("is")[-1].strip()
        memory["name"] = name
        print(f"Bot: Nice to meet you, {name}!")
        continue

    # Recall name
    if "what is my name" in user.lower():
        if "name" in memory:
            print(f"Bot: Your name is {memory['name']}.")
        else:
            print("Bot: I don't know your name yet.")
        continue

    # Time
    if "time" in user.lower():
        now = datetime.datetime.now().strftime("%H:%M:%S")
        print